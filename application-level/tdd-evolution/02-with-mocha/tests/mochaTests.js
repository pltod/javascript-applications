define([
	"underscore",
	"jquery", 
    "./01_environmentAndExecutionContext",
    "./02_dataCreation",
    "./03_dataResolution_ScopeChain",
    "./04_dataResolution_PrototypeChain",
    "./05_functionsAndExecutionContext",
    "./06_functionsAndFunctionalProgramming",
    "./07_Variables_vs_Properties",
    "./08_dataUpdate",
    "./09_dataDeletion",
    "./10_dynamicEnvironments",
    "./11_dataBinding",
	"./12_oop",
	"./oopInJS",	
	"./oopInJS2",	
	"./oopInJS3",
	"./oopInJS4",
	"./primitives", 
	"./asynch",
	"./srp",
    "./jsAPI",
	"./funjs",
    "./funjs2",
    "./funjs3",
    "./testingFlight"
], 
	function(_, $,
		Suite1, 
		Suite2, 
		Suite3, 
		Suite4,
		Suite5,
		Suite6,
        Suite7,
		Suite8,
        Suite9,
        Suite10,
        Suite11,
        Suite12,
        Suite13,
        Suite14,
        Suite15,
        Suite16,
        Suite17,
        Suite18,
        Suite19,
        Suite20,
        Suite21,
        Suite22,
        Suite23,
        Suite24
) {
	
  describe(Suite1.name, function() {
  		_.each(Suite1.tests, function(test){
  			it(test.name, test.body);
  		})
  });
  
  describe(Suite2.name, function() {
  		_.each(Suite2.tests, function(test){
  			it(test.name, test.body);
  		})
  });
  
  describe(Suite3.name, function() {
  		_.each(Suite3.tests, function(test){
  			it(test.name, test.body);
  		})
  });

  describe(Suite4.name, function() {
  		_.each(Suite4.tests, function(test){
  			it(test.name, test.body);
  		})
  });
  
  describe(Suite5.name, function() {
  		_.each(Suite5.tests, function(test){
  			it(test.name, test.body);
  		})
  });
  describe(Suite6.name, function() {
  		_.each(Suite6.tests, function(test){
  			it(test.name, test.body);
  		})
  });
  describe(Suite7.name, function() {
  		_.each(Suite7.tests, function(test){
  			it(test.name, test.body);
  		})
  });
  describe(Suite8.name, function() {
      	_.each(Suite8.tests, function(test){
  			it(test.name, test.body);
  		})
  });
  describe(Suite9.name, function() {
          _.each(Suite9.tests, function(test){
  			it(test.name, test.body);
  		})
  });
  describe(Suite10.name, function() {
          _.each(Suite10.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite11.name, function() {
          _.each(Suite11.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite12.name, function() {
          _.each(Suite12.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite13.name, function() {
          _.each(Suite13.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite14.name, function() {
          _.each(Suite14.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite15.name, function() {
          _.each(Suite15.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite16.name, function() {
          _.each(Suite16.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite17.name, function() {
          _.each(Suite17.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite18.name, function() {
          _.each(Suite18.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite19.name, function() {
          _.each(Suite19.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite20.name, function() {
          _.each(Suite20.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite21.name, function() {
          _.each(Suite21.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite22.name, function() {
          _.each(Suite22.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite23.name, function() {
          _.each(Suite23.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  describe(Suite24.name, function() {
          _.each(Suite24.tests, function(test){
      		it(test.name, test.body);
  		})
  });
  
});
